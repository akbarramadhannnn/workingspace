import React from 'react';
import {Container, Text} from './TopStyled';

const Index = ({title}) => {
  return (
    <Container>
      <Text>{title}</Text>
    </Container>
  );
};

export default Index;
